// Корень проекта — см. backend/ и frontend/
// Запуск через Docker: docker compose up --build
// Или локально:
//   cd backend && npm install && npm run dev
//   cd frontend && npm install && npm run dev
